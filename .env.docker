# Backend Environment Variables
NODE_ENV=production
PORT=5000

# Database Configuration
DB_HOST=mysql
DB_NAME=guts_db
DB_USER=guts_user
DB_PASSWORD=guts_password

# CORS Configuration
CORS_ORIGIN=http://localhost
